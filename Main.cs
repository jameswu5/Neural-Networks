using System;
using System.Collections.Generic;
using System.Linq;

namespace NeuralNetworks {
    public class Program {
        public static void Main() {
            // Recurrent.Classify.TrainLanguages(10, true);
            // Recurrent.Classify.TestNetwork();
            new Reinforcement.Game();
        }
    }
}